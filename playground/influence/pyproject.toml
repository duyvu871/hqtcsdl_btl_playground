[project]
name = "influence"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.14.2",
    "python-dotenv>=1.2.2",
    "redis>=8.0.0",
]
