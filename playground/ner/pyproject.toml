[project]
name = "crypto-ner"
version = "0.1.0"
description = "Pipeline Stage 3 — NER & coin mapping (rules + OpenRouter LLM)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pymongo>=4.6.0",
    "python-dotenv>=1.0.0",
    "openai>=1.40.0",
    "rich>=13.7.0",
]
